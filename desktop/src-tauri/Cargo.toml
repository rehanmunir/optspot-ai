[package]
name = "agent-carwash-desktop"
version = "1.0.0"
edition = "2021"
rust-version = "1.77"
description = "Native shell for the Agent Car Wash viewer"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true
